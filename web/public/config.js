// Dev placeholder. In production, /usr/share/nginx/html/config.js is rewritten
// at container start by docker-entrypoint.sh from env vars (API_BASE_URL, ...).
// For `vite dev`, apiConstants.ts falls back to VITE_API_BASE_URL then localhost.
window.__APP_CONFIG__ = {};
